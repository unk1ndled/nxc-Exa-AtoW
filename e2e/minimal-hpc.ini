[CONNECTION]
scheduler_profile = idris
ssh_host = nxc-frontend
ssh_user = root

[REMOTE]
poll_interval = 1
quick_timeout = 10
submit_timeout = 240
dry_run = false

[SCHEDULER]
submit_cmd = sbatch
run_cmd = srun
cancel_cmd = scancel
